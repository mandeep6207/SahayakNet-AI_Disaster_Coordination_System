export default function VolunteerProfilePage() {
	return (
		<main className="min-h-screen bg-slate-50 px-6 py-12 text-slate-900">
			<div className="mx-auto max-w-3xl rounded-2xl border border-slate-200 bg-white p-8 shadow-sm">
				<p className="text-sm font-semibold uppercase tracking-wide text-slate-500">Volunteer Profile</p>
				<h1 className="mt-2 text-3xl font-black text-slate-900">Profile page is ready</h1>
				<p className="mt-4 text-slate-600">
					This route now exports a valid React page, which resolves the Next.js build error.
				</p>
			</div>
		</main>
	);
}
